# Tiny Steward — Semantic Capability Graph
# Minimal dependencies, no heavy frameworks

httpx>=0.27,<1.0       # async HTTP client for LLM + embedding endpoints
pyyaml>=6.0,<7.0       # config parsing
numpy>=1.26,<3.0       # vector math for cosine similarity
